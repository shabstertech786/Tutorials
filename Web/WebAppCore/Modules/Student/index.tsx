import * as React from "react";
import * as ReactDom from "react-dom";
import Student from "../Student/src/layouts/StudentLayout"
import ReactDOM = require("react-dom");

ReactDom.render(
    <Student></Student>,
    document.getElementById("std")
);